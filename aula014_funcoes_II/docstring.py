def soma(a, b, c):
    """AI is creating summary for soma

    Args:
        a ([int]): [Valor de a]
        b ([int]): [valor de b]
        c ([int]): [valor de c]
        soma([int]): [Soma os valores a, b, c]
    """
    a = 5
    b = 10
    c = 20
    soma = a + b + c
    print (soma)